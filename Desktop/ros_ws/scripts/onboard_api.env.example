# PC FastAPI backend URL ? copy to onboard_api.env and set PC WLAN IPv4 (ipconfig).
#   cp onboard_api.env.example onboard_api.env
#   nano onboard_api.env
#
# Used by: voice nav (UI-PTT), open_onboard_web.sh, start_tour_integrated.sh
# NOT hardcoded in Python ? this file is the single source of truth on vehicle.

export COURIER_API_BASE=http://10.1.24.10:8000
export VOICE_TOUR_API_BASE="${COURIER_API_BASE}"
